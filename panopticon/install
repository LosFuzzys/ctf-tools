#!/bin/bash -ex

mkdir bin
git clone --depth=1 https://github.com/das-labor/panopticon.git
cd panopticon
cargo build --all --release
ln -s target/release/panopticon ../bin/
ln -s target/release/panop ../bin/
